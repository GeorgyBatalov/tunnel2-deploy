# ⚠️ EXAMPLE FILE - Copy to .env.secrets and fill in real values
# This file is safe to commit (contains no real secrets)
#
# Vault is DISABLED - all secrets are loaded from .env.secrets and mounted files

# Vault token (leave empty to disable Vault)
TUNNEL_Vault__Token=

# TLS Certificate configuration (PEM files mounted from ./certs/)
# Run: openssl req -x509 -newkey rsa:2048 -nodes -keyout tunnel_server.key -out tunnel_server.crt -days 365 -subj "/CN=localhost"
TUNNEL_TunnelClientTls__CertificatePath=/app/certs/tunnel_server.crt
TUNNEL_TunnelClientTls__CertificateKeyPath=/app/certs/tunnel_server.key

# JWT Signing Keys (PEM files mounted from ./env/secrets/)
# Generate: openssl genrsa -out tunnel_private.pem 4096
# Extract public: openssl rsa -in tunnel_private.pem -pubout -out tunnel_public.pem
# Files will be automatically loaded from /app/secrets/tunnel_private.pem

# Database connection string
TUNNEL_ConnectionStrings__PostgreSQL=Host=postgres;Port=5432;Database=tunnel2;Username=admin;Password=CHANGE_ME

# Redis connection string
TUNNEL_Redis__ConnectionString=redis:6379

# RabbitMQ configuration (optional - only if using RabbitMQ)
# Application user - used by tunnel_server to connect to RabbitMQ
TUNNEL_Rabbit__HostName=rabbitmq
TUNNEL_Rabbit__Port=5672
TUNNEL_Rabbit__UserName=tunnel_app
TUNNEL_Rabbit__Password=CHANGE_ME
TUNNEL_Rabbit__VirtualHost=/
TUNNEL_Rabbit__Exchange=tunnel-events
TUNNEL_Rabbit__UseSsl=false

# RabbitMQ infrastructure (for docker-compose-infrastructure.yml)
# Admin user - used for RabbitMQ management UI (http://localhost:15672)
# Generate: openssl rand -base64 24
RABBITMQ_ADMIN_USER=tunnel_admin
RABBITMQ_ADMIN_PASS=CHANGE_ME
# Application user - created by init script with limited permissions
# Generate: openssl rand -base64 24
RABBITMQ_APP_USER=tunnel_app
RABBITMQ_APP_PASS=CHANGE_ME
