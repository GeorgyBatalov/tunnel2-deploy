# ===================================================================
# Vault Configuration and Secrets - PRODUCTION
# ===================================================================
# IMPORTANT: This file contains PRODUCTION SECRETS
# This file should be named vault.env (without .template)
# This file MUST NOT be committed to git (added to .gitignore)
# ===================================================================

# ===== Vault Server Configuration =====
# Vault server address (internal docker network)
TUNNEL_Vault__Address=http://tunnel2_vault:8300

# Vault root token (generated during vault operator init)
# Get this from vault-keys.json or vault initialization output
TUNNEL_Vault__Token=hvs.XXXXX_REPLACE_WITH_ACTUAL_ROOT_TOKEN

# ===== Database Secrets =====
# PostgreSQL password for tunnel2_user
POSTGRES_PASSWORD=REPLACE_WITH_ACTUAL_POSTGRES_PASSWORD

# ===== RabbitMQ Secrets =====
# RabbitMQ password for tunnel2_rabbitmq user
RABBITMQ_PASSWORD=REPLACE_WITH_ACTUAL_RABBITMQ_PASSWORD

# ===== How to use this file =====
# 1. Copy this template: cp vault.env.template vault.env
# 2. Replace all REPLACE_WITH_* placeholders with actual secrets
# 3. Ensure vault.env is in .gitignore (it should be!)
# 4. Keep vault.env secure and backed up offline
